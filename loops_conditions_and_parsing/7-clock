#!/usr/bin/env bash
# script
count=0

for hours in {0..12}; do
    for minutes in {1..59}; do
        if [[ $count -lt 70 ]]; then
            printf "%02d:%02d\n" $hours $minutes
            count=$((count + 1))
        else
            break 2
        fi
    done
done

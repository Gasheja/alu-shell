#!/bin/bash

# Header line for process information
header="USER       PID %CPU %MEM    VSZ   RSS TTY      STAT START   TIME COMMAND"

# Get process information using the ps command
processes=$(ps aux)

# Print the header line
echo "$header"

# Print each process line by line
echo "$processes" | while IFS= read -r line; do
  echo "$line"
done

